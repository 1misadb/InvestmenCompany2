<Window x:Class="InvestmentCompany.Views.AddClientView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавить клиента" Height="300" Width="400">
    <Grid Margin="10">
        <StackPanel Orientation="Vertical">
            <Label Content="Название:" />
            <TextBox x:Name="txtName" Margin="0,5,0,10" />
            
            <Label Content="Адрес:" />
            <TextBox x:Name="txtAddress" Margin="0,5,0,10" />
            
            <Label Content="Телефон:" />
            <TextBox x:Name="txtPhone" Margin="0,5,0,10" />
            
            <Button Content="Сохранить" Click="SaveButton_Click" Margin="0,10,0,0" />
        </StackPanel>
    </Grid>
</Window>